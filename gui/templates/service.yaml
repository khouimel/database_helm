apiVersion: v1
kind: Service
metadata:
  name: gui-service
spec:
  selector:
    app: gui
  ports:
    - protocol: TCP
      port: {{ .Values.gui.service.port }}
      targetPort: {{ .Values.gui.service.targetPort }}
  type: {{ .Values.gui.service.type }}
